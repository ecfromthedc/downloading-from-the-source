<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.risingtides.voice-memos-keepalive</string>

    <!--
      TEMPLATE — install.sh regenerates this file with your real $HOME.
      Edit install.sh to change defaults; don't edit this file by hand
      (your edits will be overwritten on next install).

      Placeholders substituted by install.sh:
        __HOME__  → your $HOME (e.g. /Users/yourname)
    -->

    <!--
      Wakes Voice Memos.app every 20 min so its CloudKit subscription
      stays active and new iPhone recordings sync down to this Mac.
      Opens hidden (-j) and in the background (-g) so it never steals
      focus or shows windows.
    -->
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/open</string>
        <string>-gja</string>
        <string>/System/Applications/VoiceMemos.app</string>
    </array>

    <key>StartInterval</key>
    <integer>1200</integer>

    <key>RunAtLoad</key>
    <true/>

    <key>StandardOutPath</key>
    <string>__HOME__/Library/Logs/voice-memos-keepalive.log</string>
    <key>StandardErrorPath</key>
    <string>__HOME__/Library/Logs/voice-memos-keepalive.log</string>

    <key>LimitLoadToSessionType</key>
    <string>Aqua</string>
</dict>
</plist>
